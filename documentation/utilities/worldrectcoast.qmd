# worldrectcoast

```julia
cl = worldrectcoast(proj="?", res="crude", coastlines=nothing, limits=Float64[])
```

Return a project coastline, at `res` resolution, suitable to overlain in a grid created with the
{{< gmtref worldrectangular >}} function. Note that this function, contrary to {{< gmtref coastlinesproj >}}, returns coastline
data that spans > 360 degrees.

- `proj`: A proj4 string describing the projection (Mandatory).

- `res`: The GSHHG coastline resolution. Available options are: `crude`, `low`, `intermediate`, `high` and `full`

- `coastlines`: In alternative to the `res` option, one may pass a {{< gmtref GMTdataset type=gmttype >}} with coastlines
   previously loaded (with {{< gmtref gmtread >}}) from another source.

- `limits`: If not empty it must be a 2 elements array with lon_min, lon_max that is used to ask for
   coastlines that expand more than 360 degrees ({{< gmtref worldrectangular >}} uses this).

Returns
-------

A Vector of {{< gmtref GMTdataset type=gmttype >}} containing the projected world GSHHG coastlines at resolution `res`.


See Also
--------

{{< gmtref coastlinesproj >}}, {{< gmtref graticules >}}, [plotgrid!], {{< gmtref worldrectangular >}}, {{< gmtref worldrectgrid >}}, 
[Best rectangular world map projection]

```{julia}
#| echo: false
#| output: asis
using GMT
GMT.doc_source_links("worldrectcoast")
```
